name: Generate Video (Environment Synced)

on:
  push:
    paths:
      - "data_berita.txt"
      - "videogen_beta.py"
      - ".github/workflows/generate.yml"
      - "requirements.txt" # Tambahkan ini agar update library juga memicu action

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo ke runner
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Pastikan versi Python sama seperti lokal
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      # 3️⃣ (KRUSIAL) Install dependensi sistem untuk FONT
      - name: Install system dependencies for Font Rendering
        run: |
          sudo apt-get update
          sudo apt-get install -y libfreetype6-dev

      # 4️⃣ Install dependensi Python dari file (lebih rapi & efisien)
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          echo "✅ Library siap dipakai:"
          pip list | grep -E "moviepy|Pillow|numpy|imageio"

      # 5️⃣ Jalankan generator video
      - name: Run video generator
        run: |
          echo "🚀 Menjalankan videogen_beta.py..."
          python videogen_beta.py
          echo "🎬 Proses selesai!"
          ls -lh output_video_*.mp4 || echo "⚠️ Tidak ada video yang dihasilkan."

      # 6️⃣ Upload hasil ke tab Actions → Artifacts
      - name: Upload hasil video
        if: success() # Hanya upload jika langkah sebelumnya berhasil
        uses: actions/upload-artifact@v4
        with:
          name: videos_output
          path: output_video_*.mp4
